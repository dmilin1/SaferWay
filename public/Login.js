'use strict'

class Login extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return(
      <h1>login</h1>
    );
  }
}

ReactDOM.render(<Login/>, document.getElementById("login"));
